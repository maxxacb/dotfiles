- defaults:
    link:
      relink: true

- clean: ['~']

- link:
    ~/.gitconfig:
    ~/.nanorc:
    ~/.zshrc:

- shell:
    -
      command: ./setup_homebrew.zsh
      stdout: true
      stderr: true
